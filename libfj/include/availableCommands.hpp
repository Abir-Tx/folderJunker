#if !defined(AVAILABLECOMMANDS_HPP)
#define AVAILABLECOMMANDS_HPP

#include <iostream>

const std::string availCmd[] = {"-h, --help", "-v, --version", "-w, --word",
                                "-n, --number", "--destructive"};

#endif // AVAILABLECOMMANDS_HPP
